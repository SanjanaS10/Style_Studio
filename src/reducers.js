// src/reducers.js
const initialState = {
    // your initial state here
};

const reducer = (state = initialState, action) => {
    switch (action.type) {
        // handle actions here
        default:
            return state;
    }
};

export default reducer;
